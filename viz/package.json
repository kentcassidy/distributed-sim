{
  "name": "dff-viz",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Distributed Flight-Dynamics Federation - 3D worldspace viewer (Vue + Vite + Three.js). Reads NDJSON emitted by the C++ federates; the federate only emits, this only reads.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.169.0",
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "vite": "^5.2.0"
  }
}
