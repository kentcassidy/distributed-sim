DONE (2026-09-13 batch)
- [DONE] Click each axis on the gizmo "jumps" to a view down the + of the clicked axis, "normalizing" the orthogonal axes. (Blender-style; +Y up for the Z top-down.)
- [DONE] Isometric wall / edge / units visibility now uses the parallel view direction instead of the perspective camera point.
- [DONE] Box-boundary highlight overlap: resolved by the slab-based partition rendering -- each federate owns a distinct slab, so the boxes tile.
- [DONE] Global slider multiplier over the per-federate size slider, centered at 1x (double-click to reset).
- [DONE] World box flush with the defined worldbounds (from the controller meta); axes visible at the 0 edges.
- [DONE] NDJSON outs organized as one folder per simulation run; the viewer ingests runs + a run dropdown.
- [DONE] Per-aircraft camera tracking selector: pick an aircraft, camera zooms in and follows it; default view stays framed on the whole world.

DROPPED
- Per-federate "skeleton" button (whole-world skeleton relative to one federate). Passed on -- largely covered by the partition view + World-frame toggle.
- Adjustable viewport seams (draggable gutters). Removed from backlog.

REMAINING (feature backlog -- wait on more core features)
- Vector-field (wind/storm) overlay: the "Vector field" toggle is still a stub. (Needs the Live/environment work.)
- Ghost rendering / Live viewpoints: render role:"ghost" distinctly once the Live experiment produces dead-reckoned ghosts.
