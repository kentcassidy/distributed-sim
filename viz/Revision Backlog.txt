- Click each axis on the gizmo "jumps" to a top-down view from the + of clicked axis, "normalizing" the orientation of the remaining orthogonal vectors. This should be similar to Blender.
- The in/visibility of the outer walls is perfect when at the perspective setting. However, the calculations of wall visibility remain the same as perspective when I toggle into the isometric mode. Everything else about the isometric view is perfect, but I'd also like to toggle the calculations for the outer-wall visibility with this switcher. (Don't forget the units on nearest-wall)
- The highlights for each box boundary is currently overlapping, only showing one when all are active. This may possibly change when we establish lower inclusive / upper exclusivity as we just did in the simulation. Meaning this may fix itself, but it's still to be seen. A backup solution is to have the dashed lines offset by a smidge, so all colors are layered yet stick out when overlapped... OR, to dynamically shorten the dashes to properly fit the alternate colored dashes -- more cohesive visual but a bit more work.
- Global slider which is a multiplier for the federate size slider. Centered at 1x.
- For when it's available, another button per federate which shows/hides the "skeleton" of the entire worldspace in relation to it. s.t. the active bounds per federate, still the same displayed gridspace, will appear as it would inside the Overall. Fun to see where the relative spaces of each are. The greater worldspace skeleton can have dashed lines to represent the edges, but faces can remain invisible. Recenter camera position and zoom (but not orientation) on this specific toggle.
- it seems like at the moment, the world box is responsive to be between [0 - margin, max-ac-pos + margin]. Where is this being sourced? I'm not sure the sectors were established by the time of display creation. I would like the dimensions to be flush with the defined worldbounds (axes still visible at 0 edges), which makes accurate the dotted lines and halos.

**Update: at this time, there should now be NDJSON outs of clearly divided partitions, organized in dedicated folders per simulation trial. Please adjust accordingly.

- Per-aircraft camera tracking selector from the panel: pick an aircraft and have the camera zoom in and follow it, while the default view stays framed on the whole world. (Added 9-12-26.)