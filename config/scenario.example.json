{
  "//": "Example DFF scenario. Data, not code — the controller LOADS this and",
  "//2": "delegates the sector assignment from it (MVP). Shapes only; tune freely.",

  "scenario": "mvp-two-aircraft-split",

  "world": {
    "bounds": { "min": [-5000, -5000, 0], "max": [5000, 5000, 3000] }
  },

  "aircraft": [
    {
      "id": "AC-01",
      "params":  { "mass_kg": 1200, "wing_area_m2": 16.2 },
      "initial": { "position_m": [-1000, 0, 1500], "velocity_mps": [ 60, 0, 0] }
    },
    {
      "id": "AC-02",
      "params":  { "mass_kg": 1200, "wing_area_m2": 16.2 },
      "initial": { "position_m": [ 1000, 0, 1500], "velocity_mps": [-60, 0, 0] }
    }
  ],

  "partition": {
    "//": "K sectors + who owns each. For the co-located baseline run, use ONE",
    "//2": "sector spanning the full world bounds and one federate — that is the",
    "//3": "monolithic side of the partition-invariance test. The split below is",
    "//4": "the K=2 side: the seam at x=0 sits between two aircraft closing on it,",
    "//5": "so it exercises halo visibility and boundary handoff by construction.",
    "halo_m": 250,
    "sectors": [
      { "id": "S-WEST", "bounds": { "min": [-5000, -5000, 0], "max": [   0, 5000, 3000] } },
      { "id": "S-EAST", "bounds": { "min": [    0, -5000, 0], "max": [5000, 5000, 3000] } }
    ],
    "assignment": [
      { "sector": "S-WEST", "federate": "fed-alpha" },
      { "sector": "S-EAST", "federate": "fed-beta"  }
    ]
  }
}
