# ── C++ / CMake build output (regenerated, never committed) ──
/build/
*.o
*.a
*.obj

# ── Run output (figures/CSV regenerate from committed scripts) ──
/runs/

# ── Editor / OS cruft ──
.vscode/
.DS_Store
Thumbs.db

# ── NOTE ──
# Docker images are NOT files and never enter git — they rebuild from the
# Dockerfile. The vendored RTI tarball IS tracked on purpose, so do not ignore it:
!third_party/portico.tgz
