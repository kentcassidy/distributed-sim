DEVLOG — 2026-09-13 (Claude-generated, FRONTEND / viz)
======================================================

Separate -viz file on purpose: the core agent also logs to 9-13-26.txt, and a shared
dated file gets overwritten. This is the FRONTEND session log; the core (constructive
invariance + migration) lives in 9-13-26.txt.

Focus: frontend only (viz/). Brought the viewer in line with the now-partitioned sim_out,
built the partition RENDERING (the visual thesis), a cosmetic pass, three feature items,
and two QoL touches. No C++ changed. viz/ stays walled off from the CMake build; the
federate only EMITS NDJSON; the viewer only READS.

CONTEXT AT START
----------------
Core side had moved: each sim_out/<run>/ is one simulation trial; a controller.ndjson
carries a meta-only line with world bounds + owner-tagged sectors; federate files carry
meta (sectors:[]) + frames with role. The viewer, still expecting flat sim_out/*.ndjson,
was broken against the new layout -- that break was the opening.

WHAT WAS BUILT (all under viz/)
-------------------------------
1) RUN-AWARE INGEST + RUN SELECTOR
   - vite.config.js dev endpoint serves runs: /sim_out/index.json -> {runs}, then
     /sim_out/<run>/index.json -> {files}, then raw files. Old flat files load as "(root)".
   - loader.js: listRuns() + loadRun(run). App.vue: a Run dropdown; switching resets the
     per-run UI state + playhead.

2) CONTROLLER INTERPRETATION (timeline.js)
   - Controller source identified unambiguously by a `world` field in its meta; its world
     bounds + owner-tagged sectors are captured; it contributes NO tracks and is not a
     federate. Fleet = federates that fly aircraft PLUS any sector owner (an empty-slab
     owner still shows). Names sorted for stable colors. Timeline gained .world + .sectors.

3) PARTITION RENDERING (SceneController.js + worldspace.js) -- the thesis
   - Pane mode from its filter: exactly one federate (owning a slab) = PARTITION view;
     all/multiple = FUSED view.
   - Fused: whole-world gridded, shaded room, flush to controller `world`.
   - Partition: whole world drops to a quiet edge wireframe (+axes); that federate's own
     slab is the lit gridded room, walls tinted toward its owner hue.
   - "Sectors" toggle = global reveal (every slab's fat edges + faint face tint).
   - Per-federate "highlight bounds" = solid FAT edges (LineSegments2, ~1px thicker than
     WebGL basic lines allow), filter-independent (light up OTHER partitions from a pane).
   - Camera: fused frames the whole world; partition frames its slab.

4) COSMETIC PASS
   - Always-on outer EDGES: buildEdgeFrame draws all 12 limit edges, never culled. Near
     edges (shared by two hidden faces) render solid but faint (opacity 0.25); far edges
     full. Slab edges use the OWNER color so they separate from the grey world wireframe.
   - ISOMETRIC FIX: wall culling, the near/far edge test, AND the units' nearest-edge pick
     now use the orthographic view DIRECTION (getWorldDirection), not the camera point.
     One helper, faceNear(), drives all three so perspective + isometric agree.
   - Units: closer to the edge (offset 0.015 -> 0.004 of extent); in a partition pane they
     attach to the SLAB bounds (per-partition units), world in fused.
   - Toggles: "World frame" (hide the whole-world wireframe in a slab view, keep the axes)
     and "Tint" (owner-hue tinting globally). Partition tint reduced ~30% (0.22 -> 0.15).

5) FEATURES (backlog #1, #2, #4)
   - #2 Global size multiplier: slider (exp map, centered 1x, 0.125x..8x, dbl-click reset)
     scaling every federate marker on top of their own sliders.
   - #1 Gizmo axis-click: click a gizmo axis knob to snap the view down that axis (X/Y keep
     Z up; Z is top-down with +Y up). Capture-phase hit-test with an invisible larger target
     per knob; stopPropagation so it doesn't also start an orbit drag.
   - #4 Per-aircraft camera tracking: "Track camera" per aircraft; zooms in and follows it
     each frame (preserving relative orbit/zoom); default framing stays the whole world.
     Only acts in panes showing that aircraft; clears on run switch.

6) QoL
   - Per-aircraft ORIENTATION in the stats: heading/pitch/roll (deg) from the reported
     quaternion (config.js quatToEulerDeg; Z-up, nose +X). Longitudinal filler shows pitch
     ~1 deg, heading/roll ~0.
   - Units DECLUTTER: _updateLabels thins tick labels by on-screen spacing each frame
     (project -> hide any within MIN_LABEL_PX=22 of the last kept), so zoomed-out views keep
     only spaced indicators and fill back in on zoom-in.
   - Halo margin bug spotted + made uniform (constant absolute gap on every face instead of
     per-axis %, which had ballooned the long-edge ends) -- but see ON HOLD below.

7) HANDOFF ATTRIBUTION (owner-per-frame) -- landed once the core emitted `owner`
   Core added an explicit per-aircraft `owner` per NDJSON row that flips at the crossover
   step (verified on run k2, seam y=750: id1 A->B and id2 B->A both at t=6.0; a track now
   spans BOTH files). Viewer updated to honor it:
   - timeline.js: carries owner-per-step (reordered on sort); stamps the HOME federate as the
     EARLIEST owner (owner[0] after sort), fixing id2 wrongly grouping under A (its t=0 row is
     B's); sample(t) returns the live owner as a step function (floor of the crossover).
   - SceneController: _styleMesh is keyed to the CURRENT owner -- color, size, and per-federate
     PANE VISIBILITY all follow it, updated every frame in _applyTime. So a plane recolors at
     t=6 and, in Separated - All, LEAVES its old federate's pane and APPEARS in the new one.
     Tracking (_canTrack/_zoom) uses the live owner too.
   - App.vue: the aircraft-panel dot recolors live (ownerCss); the stat shows "owner" with a
     "<= home" marker once it differs from where it started.
   - No geometry inference -- purely the real `owner` field. Missing owner falls back to the
     file's federate (older/non-handoff runs unaffected).

8) HANDOFF QoL
   - Highlight: added a faint RED selection outline (aircraftMesh.js: EdgesGeometry overlay,
     0xff3b30 @ 0.5, depthTest OFF so it reads from any camera angle; a child of the mesh so it
     follows transform/scale). Shown only in "highlight" mode, on top of the size + emissive.
   - Handoff diagram: on a forward-playback owner flip, each pane floats a small
     [from-color circle] -> [to-color circle] above the aircraft (federate dot colors), holds
     ~0.35s then fades by ~1.1s (CSS2D). Gated on playback.playing so scrubbing doesn't spam it;
     per-pane; cleared on run switch/dispose. Knobs: HANDOFF_FX_MS / HANDOFF_FX_HOLD + the 0.05
     above-offset; outline color/opacity in aircraftMesh.js.

9) POLISH / TUNING (final values after user review)
   - Handoff diagram timing: holds full for 1.4s then a ~0.4s fade (HANDOFF_FX_HOLD=1400,
     HANDOFF_FX_MS=1800).
   - Highlight size gain toned down 1.7x -> 1.2x (HIGHLIGHT_SCALE); brightness unchanged.
   - Red selection outline is now a FAT line (LineSegments2 @ linewidth 2, 0xff3b30 @ 0.55,
     depthTest off) so it's genuinely ~1px thicker; pixel-width (constant regardless of marker
     scale). SceneController._applyResolution keeps each mesh outline's LineMaterial.resolution
     current on resize.
   - Global size multiplier range widened: base 8 -> 16 (0.0625x .. 16x, still centered 1x,
     dbl-click resets).
   - Per-federate size row: added a reset-to-default (40 m) button; slider squashed (min-width:0,
     tighter gap) + a fixed-width right-aligned value column so the "m" no longer clips.
   - Arrow-key transport now steps by the program dt on the dt grid (snap to round(t/dt), then
     +/- n frames; Shift=10, Ctrl=50) instead of 1ms JS-render nudges.

ORIENTATION NOTE (answered, no change)
   The paper-plane marker copies the emitted quaternion each frame, so orientation IS rendered.
   A strafing aircraft (vel has +Y) still points +X because the filler physics emits a quat with
   PITCH only (rotation about Y), heading/roll = 0 -- the lateral motion is pure translation in
   the truth. So it's the data, not a render gap. A "point along velocity" marker mode is possible
   but would be viewer-invented (diverges from the emitted quaternion); left OFF unless requested.

ON HOLD -- expectations pending core data (viewer must NOT synthesize)
---------------------------------------------------------------------
- HALO from meta: the halo region is SOURCE DATA the federates should define in meta, NOT
  generated in the viewer. Current halo is a uniform-gap placeholder (internal note left in
  SceneController._buildOverlays). Expected once near-seam collision lands; drive it from the
  meta then.
- HANDOFF ownership in the NDJSON: RESOLVED (see section 7). Was on hold pending the core's
  explicit per-frame `owner`; that landed, and the viewer now follows it. Left here only as the
  trail of how it was diagnosed (M2 "report only" -> M4/M5 explicit owner).

BACKLOG STATUS (viz/Revision Backlog.txt + docs/other/viz-kanban.md)
--------------------------------------------------------------------
- DONE this session: run ingest + dropdown; partition rendering; near-edge fade + always-on
  edges; isometric visibility; gizmo axis-snap; global size multiplier; per-aircraft tracking;
  orientation stat; units declutter; HANDOFF owner-per-frame attribution (live recolor +
  pane-hop + panel owner); highlight red outline; handoff "owner->owner" diagram.
- DROPPED (user): per-federate "skeleton" button; adjustable viewport seams.
- REMAINING (wait on core/Live): vector-field overlay (stub); ghost rendering / Live
  viewpoints; halo-from-meta (source data, not synthesized).

HANDOFF
-------
Frontend at a strong stopping point; user set on frontend for now. Handoff attribution is now
DONE end-to-end (core emits `owner`, viewer follows it). Remaining frontend work is gated on
core/Live data: (a) halo geometry in meta, (b) Live experiment (ghosts, vector field).
